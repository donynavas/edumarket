# Copiar este archivo a ".env" (sin ".example") y completar con las
# credenciales reales de cada entorno. El archivo ".env" nunca debe
# subirse al control de versiones — está listado en .gitignore.

DB_HOST=127.0.0.1
DB_NAME=educacion_plus
DB_USER=root
DB_PASS=
